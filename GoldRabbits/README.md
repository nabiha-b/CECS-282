This program creates a BigInt class that handles numbers greater than 2^31-1. It handles addition, subtraction, constructor, 
copy constructor, assignment operator, destructor and other functions as needed.
